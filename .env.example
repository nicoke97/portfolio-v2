# GitHub token for the activity graph (public + private commits)
# Classic: https://github.com/settings/tokens  scopes: read:user, repo
# Fine-grained: Profile → Read, Contents → Read on your repositories
# Without this, only public contributions are shown.
GITHUB_TOKEN=
