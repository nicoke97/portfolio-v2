# GitHub token for the activity graph
# Classic: https://github.com/settings/tokens  (scope: read:user)
# Fine-grained: Account permissions → Profile → Read
# Without this, only public contributions are shown.
GITHUB_TOKEN=
